# Local browser-preview monolith, regenerated by `python3 build.py --preview`.
# Not pushed to Apps Script (see .claspignore), not part of source.
preview.html
.DS_Store

# Real clasp config carries the live scriptId — stays local. Track .clasp.json.example instead.
.clasp.json

# Throwaway openpyxl demo of the quotation template fill — regenerable, not source.
_demo-filled-template.xlsx
